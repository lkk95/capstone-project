import styled from 'styled-components';

const StyledList = styled.ul`
	list-style: none;
	margin: 2em auto;
	padding: 0;
	font-weight: 200;
	input[type='checkbox'] {
		margin-right: 1em;
	}
`;

export default StyledList;
