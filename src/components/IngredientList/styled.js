import styled from 'styled-components';

const StyledList = styled.ul`
	margin: 2em auto;
	padding: 0;
	font-weight: 200;
	list-style: none;
	input[type='checkbox'] {
		margin-right: 1em;
	}
`;

export default StyledList;
