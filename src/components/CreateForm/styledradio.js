import styled from 'styled-components';

const StyledRadio = styled.fieldset`
	display: flex;
	justify-content: space-evenly;
	margin-top: 1em;
	border: 0;
	text-align: center;
`;

export default StyledRadio;
